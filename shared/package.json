{
    "name": "shared",
    "version": "1.0.0",
    "description": "Shared types and utilities for electronics store",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "clean": "rimraf dist",
        "type-check": "tsc --noEmit"
    },
    "devDependencies": {
        "rimraf": "^5.0.5",
        "typescript": "^5.3.2"
    }
}